import GithubRepository from "./ui/github-repository";
import SNS from "./ui/sns";

const Footer = () => {
  return (
    <footer>
      <GithubRepository />
      <SNS />
    </footer>
  );
};

export default Footer;
